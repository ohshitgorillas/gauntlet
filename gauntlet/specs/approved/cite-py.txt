slug: cite-py
kind: characterization
brief:
> scripts/cite.py — citation resolver for the plan gate. Fact reporter, no verdicts, same contract as gauntlet-detective.
> Problem: tree is frozen during drafting and review, so citation errors are mis-transcription, not drift — detective table retyped wrong, grep-hit line cited instead of the construct, bare :49 inheriting the wrong file, off-by-one ranges, citing from memory. All deterministic, all checkable.
> Three modes: --resolve DOC (prosecutor, table of every citation with its actual line text, replaces N Read calls with one command); --check DOC (main agent, pre-flight, exit 1 on MISSING, RANGE, AMBIGUOUS, quote mismatch); --fix DOC (author, fills the number from the quoted anchor where the hit is unique, refuses on zero or multiple).
> Parser is the hard part: bare :49 continuation (must print INHERITED-FROM <file>), basename-only, ranges, cross-repo paths. Prose numbers without backticks out of scope.
> Guarantee: the number points at a line containing that quote. Nothing more.
> if the citations are automatically generated, why are we spending tokens re-resolving them
> doesn't happen, what are you talking about?
> Build it.
> cut --resolve, ok by me, approved
> how is a quoted anchor written beside a citation — same line, either side

surface: `scripts/cite.py`, invoked as `python3 scripts/cite.py --check <doc>` and
`python3 scripts/cite.py --fix <doc>`. `--check` prints one row per reported citation on
stdout and exits 1 where any failing row is present, 0 otherwise. A citation is a
backticked `path:line` or `path:line-line`; the path may be omitted to continue the
previous one. The checkout root is resolved from the script's own path, not the cwd.

A citation carries a quoted anchor where a double-quoted run of text sits on the same
source line of the document as the citation, on either side of it, separated from it by
whitespace and punctuation only; where that line carries several such runs, the one
nearest the citation is its anchor. A double-quoted run on any other source line of the
document anchors nothing, whatever the paragraph it shares with the citation. A citation
whose source line carries no double-quoted run carries no anchor.

Each line below cites one file by an absolute path the test writes outside the checkout,
and the file is rewritten in place between that line's two runs. The citation string does
not change, so the document is byte-identical across both runs and only the cited file's
contents differ.

1. For one document carrying the single citation `` `<abs>/target.txt:2` `` with the
   anchor "the anchored line" quoted beside it, handed to `--check` twice: where
   `target.txt` carries that text on its line 2, it exits 0; where the same
   `target.txt` carries that text on its line 3 and different text on its line 2, it
   exits 1 with a row naming `QUOTE`.
   kills: searches the whole file for the anchor rather than the cited line, so an
     off-by-one citation whose text sits one line away passes
   bite: measured at HEAD with the repo's own script,
     `python3 /home/atom/dev/gauntlet/scripts/cite.py --check doc.md`: anchor on line 2
     -> exit 0, one row ``CROSS-REPO  `<abs>/target.txt:2`  <abs>/target.txt``; anchor on
     line 3 -> exit 1, one row
     ``QUOTE  `<abs>/target.txt:2`  <abs>/target.txt  something else here``
   existing: none, no test names `QUOTE`

2. For one document carrying the single citation `` `<abs>/target.txt:2-3` `` with the
   anchor "the anchored line" quoted beside it, handed to `--check` twice: where
   `target.txt` carries that text on its line 3, it exits 0; where the same
   `target.txt` carries that text on its line 5 and on neither line 2 nor line 3, it
   exits 1 with a row naming `QUOTE`.
   kills: compares the anchor against a span's first line only, so a correct citation to
     a two-line construct quoted from its second line fails
   bite: measured at HEAD with the repo's own script,
     `python3 /home/atom/dev/gauntlet/scripts/cite.py --check doc.md`: anchor on line 3
     -> exit 0, one row ``CROSS-REPO  `<abs>/target.txt:2-3`  <abs>/target.txt``; anchor
     on line 5 -> exit 1, one row
     ``QUOTE  `<abs>/target.txt:2-3`  <abs>/target.txt  two``
   existing: none, no test names `QUOTE`
   past the first line: line 1 pins a quote against a single cited line, and a
     first-line-only comparison satisfies it while rejecting a correct citation to a
     two-line construct, which is how a range is written.

3. For one document carrying the single citation `` `<abs>/target.txt:1` `` with the
   anchor "the target line" quoted beside it, where line 1 of `target.txt` does not
   carry that text, handed to `--fix` twice: where `target.txt` carries that text on its
   line 4 and nowhere else, the document afterwards carries the citation
   `` `<abs>/target.txt:4` ``; where `target.txt` carries that text on its line 4 and
   again on its line 6, the document afterwards is byte-identical to the document
   before.
   kills: fills the number from the first line the anchor matches, so an anchor
     occurring twice is resolved to an arbitrary one of them
   bite: measured at HEAD with the repo's own script,
     `python3 /home/atom/dev/gauntlet/scripts/cite.py --fix doc.md`: anchor on line 4
     only -> exit 0, one row
     ``FIXED  `<abs>/target.txt:1`  -> `<abs>/target.txt:4```, and the document
     afterwards reads ``The rule holds `<abs>/target.txt:4`, "the target line" is what
     it carries.``; anchor on lines 4 and 6 -> exit 0, one row
     ``REFUSED  `<abs>/target.txt:1`  the anchor is not unique``, and the document
     afterwards is byte-identical to the document before
   existing: none, no test names `--fix`
   past the first line: lines 1 and 2 read a document and decide an exit code. This is
     the second mode and it writes, and the brief's refusal — "refuses on zero or
     multiple" — is a behavior of that mode alone.

--- reviewer ---

READY
discriminates: sweep on the cited file's contents with the citing document held byte-identical - `--check` exit code and status row on 1 and 2, the document `--fix` leaves behind on 3

1  KEEP  one document citing `<abs>/target.txt:2` with the anchor "the anchored line" beside it, run twice over the same path rewritten in place -> anchor on line 2 exits 0, anchor on line 3 exits 1 with a row naming `QUOTE`; whole-file anchor search fails it at the second run, where the text sits one line off and it still exits 0; bite: measured at HEAD, `python3 /home/atom/dev/gauntlet/scripts/cite.py --check doc.md`, exit 0 with a `CROSS-REPO` row and exit 1 with a `QUOTE` row
2  KEEP  the same document citing the two-line span `<abs>/target.txt:2-3` -> anchor on line 3 exits 0, anchor on line 5 and on neither span line exits 1 with a row naming `QUOTE`; first-line-only comparison fails it at the first run, rejecting a correct citation quoted from the span's second line; bite: measured at HEAD, same command, exit 0 with a `CROSS-REPO` row and exit 1 with a `QUOTE` row
3  KEEP  one document citing `<abs>/target.txt:1` with the anchor "the target line" beside it, handed to `--fix` over the same path rewritten in place -> anchor unique on line 4 leaves the document carrying `<abs>/target.txt:4`, anchor on lines 4 and 6 leaves the document byte-identical to the one handed in; first-match fill fails it at the second run, where it writes `:4` into a document that must come back unchanged; bite: measured at HEAD, `python3 /home/atom/dev/gauntlet/scripts/cite.py --fix doc.md`, a `FIXED` row with the rewritten document and a `REFUSED` row with the document unchanged

null stub: `scripts/cite.py` importable, parsing nothing, printing no rows, exiting 0 on every document, leaving every document byte-identical.
  satisfies: none   fails: 1, 2, 3 (each line's second run demands exit 1, and line 3's first run demands a rewritten document)
hard-coded stub: a table from the citing document's literal text to an exit code, a status row and (for `--fix`) an output document; opens no cited file, stats nothing.
  satisfies: none   fails: 1, 2, 3 (each line hands in one byte-identical document across both runs and expects two different results, so the only thing that can decide is the cited file)

survives: 3

note, line 3's second run: "byte-identical to the document before" is the document's whole
content named by identity, not an absence - the first run of the same line demands a
rewrite, so no stub passes the line by doing nothing. It stands as one comparison over
full state, which is the form (h') asks for.

note, line 1 and line 2's passing run: the measured bite shows a `CROSS-REPO` row on the
run that exits 0, because the cited path is outside the checkout. The lines pin the exit
code there and say nothing about that row, which is right - `CROSS-REPO` is already pinned
at tests/test_cite.py:180 and must not be pinned a second time here. A test that compares
a status-token list rather than the exit code on those runs would be pinning it twice.
