slug: blind-sh-run
kind: characterization
brief:
> - blind.sh: zero execution tests. tests/test_tests_config.py:520 greps script text only. Defects fixed today were invisible to the gate for this reason.

1. Where the command text is held at `scripts/blind.sh test .claude/worktrees/<slug>-spec/tests/<file>`, its exit status is 0 where that worktree's copy of `<file>` holds a passing test and the main checkout's namesake under the tests directory holds a failing one, and 1 where that worktree's copy holds a failing test and the main checkout's namesake holds a passing one.
   kills: a path naming a spec worktree resolved against the main checkout, so a blind writer's run reports on the file it did not write and calls its own red run green; and a dispatcher keyed on the argument string, which answers every `.claude/worktrees/` path with one status without reading the file the path names.
   bite: characterization, `docs/testing.md` rule 8 exempts the bite, said and not assumed: every line in this block states behavior HEAD already has, so the red run is green.
   existing: none, `grep -rn "blind.sh\|BLIND_SH" tests/ --include=*.py` -> tests/test_pair_sh_merge.py:486 a comment, tests/test_gates_wired.py:105 the gate array, tests/test_tests_config.py:7,29,228,237,238,248,250,520,526,539 reads of the script's text or of a hook's command pattern, and no call that runs the script

3. Where a slug's spec branch carries a different text for that slug's approved spec file than the main checkout's HEAD carries, the stdout of `scripts/blind.sh show HEAD <slug>` is the spec worktree's text where that worktree exists on disk and the main checkout's text where it does not.
   kills: the tree resolved for `status` and the main checkout taken for `show`, so a reviewer reading a block back gets the text of a tree other than the one it was told the block is committed in.
   bite: characterization, `docs/testing.md` rule 8 exempts the bite, said and not assumed.
   existing: none, same grep as line 1

4. Where two git checkouts each carry a committed approved spec file for the same slug with different text, and neither carries a worktree for that slug, the stdout of `scripts/blind.sh show HEAD <slug>` is the first checkout's text where the run's working directory is inside the first checkout and the second checkout's text where it is inside the second.
   kills: a script resolving the block from a checkout fixed at the script's own location rather than from the caller's, so one installed kit answers every project with one project's block. It is separable from line 3: an implementation resolving the tree from `git rev-parse --show-toplevel` satisfies this line at both its inputs, both of them main checkouts, and fails line 3 at the worktree-exists input.
   bite: characterization, `docs/testing.md` rule 8 exempts the bite, said and not assumed.
   existing: none, same grep as line 1

--- reviewer ---
READY
discriminates: sweep on `blind.sh test` (line 1, two exit statuses on one fixed command as the worktree copy varies), plus differentials on the tree `blind.sh show` resolves (lines 3 and 4)

1  ADMITTED  `scripts/blind.sh test .claude/worktrees/<slug>-spec/tests/<file>`, command text fixed -> exit 0 where the worktree copy passes and the main checkout's namesake fails, exit 1 where the two are swapped; a run resolving that path against the main checkout exits 1 at the first input and 0 at the second, failing both arms; bite: characterization, `docs/testing.md` rule 8 exemption stated on the line, not assumed.
2  STRICKEN  hard-coded stub: the two arms differ by whether the worktree's copy exists, so `[ -e <worktree>/<spec file> ] && echo " M <path>" || echo " D <path>"` satisfies both without consulting any git index. Repair: keep the worktree copy present in both arms and vary its git state instead — tracked-and-modified against present-but-untracked, whose two porcelain codes no existence probe can tell apart.
3  ADMITTED  carried. `scripts/blind.sh show HEAD <slug>` with the spec branch carrying different text than main's HEAD -> the spec worktree's text where that worktree exists on disk, the main checkout's text where it does not; a `show` that always reads the main checkout fails it at the worktree-exists input; bite: characterization, `docs/testing.md` rule 8 exemption stated on the line, not assumed.
4  ADMITTED  `scripts/blind.sh show HEAD <slug>` run from inside each of two checkouts that carry different committed texts for the same slug and no worktree -> each checkout's own text; a script resolving the block from a checkout fixed at its own install location prints one text at both inputs and fails one of them; bite: characterization, `docs/testing.md` rule 8 exemption stated on the line, not assumed. The two working directories are fixture checkouts the test builds, not the ambient one, so rule 16 is not engaged.

null stub: `blind.sh` present, every subcommand a no-op that prints nothing and exits 0.
  satisfies: none   fails: 1, 2, 3, 4
hard-coded stub: branch on the argument string and on the existence of paths under the named worktree — exit a fixed status for a `.claude/worktrees/` argument, print ` M <path>` or ` D <path>` by whether the worktree's spec file exists, print a fixed text for `show` — reading no git state and running no test.
  satisfies: 2   fails: 1, 3, 4

survives: 3
