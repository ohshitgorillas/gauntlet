slug: symbol-closure
grounding: gauntlet-detective

brief:
> Add scripts/gates/symbol-closure.py: an AST scan of tests/ that takes symbol names and returns the test files that import or reference them, one transitive hop through tests/support, file granularity and no arity analysis, with a --self-test and an entry in scripts/gates/check-gates.sh.
  delivered: the first element of "Which files get touched", which fixes the path the owner named, the scan, the one hop, the two stated bounds and the `--self-test`; and the second element, the fourteenth entry in `scripts/gates/check-gates.sh`.
> Wire it for the gauntlet-arbiter as a declared-run entry in .claude/hooks/blind-reads.json whose argument shape is pinned under tests/, and add one arbiter check: derive the symbol set from the block's own bite: and existing: text, run the closure, and require every file it returns to be named by an existing: clause or excise target in that block.
  delivered: the `blind-reads.json` element, with `prefix: "tests"`; and the block-level check element, which carries the derivation rule, the matching rule and the four boundary readings.
> existing: now answers two questions rather than one — the outcome grep that says what already pins the line, and the closure that says what the change reaches — so docs/approved-specs.md and .claude/agents/gauntlet-arbiter.md:45 both change with it.
  delivered: the `.claude/agents/gauntlet-arbiter.md:45` and `.claude/agents/gauntlet-arbiter.md:48` elements and the `docs/approved-specs.md` element. The `.claude/agents/gauntlet-arbiter.md:45` element carries the list grammar the second question forces; the `.claude/agents/gauntlet-arbiter.md:48` and `docs/approved-specs.md` elements state the two questions in those words.
> Deliver a stage 1 plan with a READY verdict.
  delivered: this plan and the round it is in.

Nothing is dropped, so Open questions stays `None`.

## What is wrong

`existing:` answers one question today, and the arbiter's check (b) is the whole of it. `.claude/agents/gauntlet-arbiter.md:110` tells the reviewer to grep `tests/` "for outcome line state, whatever main agent wrote", and the verdict it produces is `AMENDED <file:line>` where a test already pins that outcome and an escape where none does. That question is backward-looking: what already holds this line. `.claude/agents/gauntlet-arbiter.md:130` adds check (l), which only demands the citation name a test rather than a file or a range. `docs/approved-specs.md:38` states the field's grammar and nothing more.

Nothing in the chain asks the forward question: which tests the approved change reaches. A behavior line that renames or re-types a symbol four test files import passes review naming the one file whose outcome the main agent happened to grep for. The `gauntlet-scrivener` then writes against that one file, the red run goes red where it was told to look, and the other three files break at the merge, where the `gauntlet-bailiff` rules on the block's own behavior lines and has no clause that would name them.

The reviewer cannot close that gap by reading. It may read `tests/` — `.claude/agents/gauntlet-arbiter.md:62` grants every file under that directory — but a reviewer grepping for a symbol name by hand gets string hits, not references, and `.claude/agents/gauntlet-arbiter.md:23` makes the default verdict `STRICKEN`, so a check the reviewer cannot run cheaply is a check that decays into discretion. No tooling exists to run instead: the grounding round found no `import ast` anywhere under `.claude/` or `scripts/`, so there is no reference scanner in this repo at all.

The reviewer also has the shell to run one with. `.claude/agents/gauntlet-arbiter.md:4` is `tools: Read, Grep, Glob, Bash, Write`, and the frontmatter at `.claude/agents/gauntlet-arbiter.md:6-19` wires three hooks on it. Two tree facts contradict that and are wrong. `.claude/hooks/blind-bash.py:9` says the `gauntlet-juror` and the `gauntlet-arbiter` have no `Bash` at all; that hook is wired from the scrivener's and the bailiff's frontmatter and from nowhere else, per `.claude/hooks/blind-bash.py:4-5`, so it never answers for the arbiter. `docs/agents.md:26` carries the same association from the other side, naming `blind-bash.py` as one of the hooks whose silencing lets a bypassed `gauntlet-arbiter` "run any shell command"; the hooks that actually bound the arbiter's shell are `.claude/hooks/reviews-lane.py`, `.claude/hooks/no-impl-reads.py` and `.claude/hooks/specs-lane.py`, which `.claude/agents/gauntlet-arbiter.md:16-19` wires on a matcher carrying `Bash` and which routes a `Bash` call at `.claude/hooks/specs-lane.py:79-80` to the `_bash_verdict` defined at `.claude/hooks/specs-lane.py:68`, plus `.claude/hooks/bwrap-wrap.py`, which takes every `Bash` call in the session. Neither sentence changes any behavior, and both are stale prose a consumer reads; the whole of this plan rests on the reading they deny.

## What the owner sees change

A draft spec block no longer reaches `READY` while it reaches test files it does not name. The arbiter runs one command per block, gets back the set of test files the block's own symbols reach, and returns `ANOTHER PASS` naming each file the block left unnamed. The repair is the main agent's: name the file in an `existing:` clause, or excise it on an amend line.

`existing:` carries more than one clause from here on. Today it is one grep result or one `tests/<file>::<test>` citation; it becomes a list, because the closure's answer is a set and every member of it has to be named somewhere in the block.

Green grows by one gate. `scripts/gates/check-gates.sh` runs fourteen where it ran thirteen, and both places that count them — `CLAUDE.md:17` and `scripts/gates/check-gates.sh:2` — say fourteen.

A consumer copying `.claude/` and `docs/` gets a changed `gauntlet-arbiter` and a `blind-reads.json` naming a second repo-local runner, and needs `scripts/gates/symbol-closure.py` as well to get the check. A consumer who copies neither the entry nor the script gets the arbiter it has today: the check reads `.claude/hooks/blind-reads.json` for its own entry and prints `N/A` where there is none, so it holds no block in a repo that never wired it. `docs/approved-specs.md:84` already says a repo that writes nothing in that file "assumes nothing, which is why the file may be absent", and this check is built on that reading rather than against it.

What does not change: the arbiter stays blind, the per-line checks (a) through (t) keep their letters and their verdicts, and the closure never reads anything outside `tests/`.

## Which files get touched, and roughly how

**`scripts/gates/symbol-closure.py`, new.** A `python3` script with a shebang and the executable bit, since the declared-run key is the head word of the invocation and `.claude/hooks/shell_shapes.py:379` normalizes `words[0]` against the declared entry — `python3 scripts/gates/symbol-closure.py` has `python3` in that position and is not a declared run. It parses every `*.py` file under the `tests/` tree with `ast.parse`, walks each module once, and reports the files whose module-level `Import`/`ImportFrom` names, or whose `Name`/`Attribute` identifiers, are in the symbol set. Granularity is the file: a hit anywhere in a module puts the module's path in the output, and no call site is matched against a signature, so a symbol used with the wrong arity is a hit like any other. The transitive hop is one deep and runs through `tests/support` only: a support module that itself references a seed symbol is added to the closure, and the test files that reference that support module's own exported names are added with it. Nothing beyond that second level is followed. Output is one repo-relative path per line on stdout, sorted, and an exit code of 0 whether the set is empty or not — this is a fact reporter, not a gate, and the verdict on its output is the arbiter's.

Symbols arrive on stdin, one per line, because argv has no room for them: `.claude/hooks/shell_shapes.py:383` is `if len(rest) != len(args) + 1 or list(rest[: len(args)]) != list(args):`, so a declared run carries the fixed argument words and then exactly one argument. That one argument is the anchor path, and it is what the brief pins under `tests/`. The script takes the `tests/` directory the anchor sits in as its scan root, so the anchor identifies the tree rather than the scan. The arbiter's fixed invocation is `scripts/gates/symbol-closure.py --symbols-stdin tests/__init__.py`, reached as `printf '%s\n' <symbol> <symbol> | scripts/gates/symbol-closure.py --symbols-stdin tests/__init__.py`. That anchor is tracked: `git ls-files tests/__init__.py` prints `tests/__init__.py`.

`--self-test` follows the convention the hooks use: one `PASS` or `FAIL` line per rule the script exists to hold, and a non-zero exit if any failed, as `.claude/hooks/no-impl-reads.py:604` prints it and `.claude/hooks/no-impl-reads.py:609` dispatches it. Its rules are the ones the description names: a direct import is a hit, a bare reference is a hit, a support-module hop is a hit, a second hop is not, a same-named symbol in an unrelated file is a hit (file granularity, no arity analysis, stated as a bound rather than hidden), a syntactically broken file is reported and not fatal, and the anchor's tree is the scan root. It builds its fixtures in a temporary directory, so it does not read or write the real `tests/`.

**`scripts/gates/check-gates.sh:35-48`.** One more entry in the `gates=(...)` array, in the shape of `scripts/gates/check-gates.sh:36`, appended after `scripts/gates/check-gates.sh:48`: `"symbol-closure|python3 scripts/gates/symbol-closure.py --self-test"`. The runner beneath it already writes `state/gates/<gate>.txt` and already prints one `PASS` or `FAIL` line per gate, so no logic in that script moves. One comment does: `scripts/gates/check-gates.sh:2` is "# Run the thirteen gates of this repository, one after another, at idle priority." and changes with `CLAUDE.md:17`.

**`CLAUDE.md:17`** is "Green means all thirteen gates pass, not just the first. One command runs them:" and says fourteen.

**`.claude/hooks/blind-reads.json:3`** gains a second entry beside the existing one: `{"entry": "scripts/gates/symbol-closure.py", "args": ["--symbols-stdin"], "prefix": "tests"}`. Every field is the shape `.claude/hooks/shell_shapes.py:347` reads, the prefix is not a lane directory so it survives the drop at `.claude/hooks/shell_shapes.py:355`, and `tests` is the prefix `.claude/hooks/shell_shapes.py:311` already expands for this repo's other entry.

The same file gains a second top-level key, `"allow": [".claude/hooks/blind-reads.json"]`, which is what lets the arbiter tell an unwired repo from a broken one. `.claude/hooks/no-impl-reads.py:156` is `allow = tuple(DEFAULT_ALLOW) + tuple(conf.get("allow") or ())`, and `.claude/hooks/no-impl-reads.py:88` is the default list, which reaches nothing under `.claude/`; without the key the arbiter cannot read the file it is declared in. The entry is the one `.claude/hooks/no-impl-reads.py:112-113` invites — "add it to the allow list in " — and it is a single file rather than a directory, so it opens `.claude/hooks/` no wider. The carve-out at `.claude/hooks/no-impl-reads.py:205-209` is untouched by it: that one refuses any allow entry reaching under `gauntlet/`, and this path is not under it. The reach is wider than the arbiter, and the plan says so rather than implying otherwise: `.claude/hooks/no-impl-reads.py:156` builds the allow list per config and not per agent, so the file becomes readable to the `gauntlet-scrivener`, the `gauntlet-juror` and the `gauntlet-bailiff` as well. It carries `runner_invocations` and this key and no implementation citation, so `docs/agents.md:24` still holds for all four.

**`.claude/agents/gauntlet-arbiter.md:45`**, the `existing:` grammar line inside the `Inputs` fence, becomes a list form rather than a single clause: `existing:` takes one or more comma-separated clauses, each of them the grep-and-result form or a `tests/<file>::<test>` citation. **`.claude/agents/gauntlet-arbiter.md:48`** is the paragraph that says `bite:` and `existing:` carry evidence; it gains the second question in one sentence — the outcome grep says what already pins the line, the closure says what the change reaches — and names the block-level check below.

**One new block-level check in `.claude/agents/gauntlet-arbiter.md`,** written in that file's compressed register and placed beside the other block-level check rather than among the per-line letters. The discrimination check at `.claude/agents/gauntlet-arbiter.md:64-74` is block-level and runs before the stubs; the closure check runs after the per-line letters end at `.claude/agents/gauntlet-arbiter.md:148`, because its input is every surviving line's fields. It carries five things, and the boundaries are part of the text rather than left to the reviewer:

- **Derivation.** The symbol set is every backtick-quoted token in the surviving lines' `bite:` and `existing:` text that is a legal Python identifier or a dotted path of them, plus the `<test>` half of every `tests/<file>::<test>` citation in those fields. Unbackticked prose contributes nothing: the reviewer is reading fields the main agent wrote, and a rule that harvested English words would return the whole suite.
- **The run.** `printf '%s\n' <symbols> | scripts/gates/symbol-closure.py --symbols-stdin tests/__init__.py`, once per round, after the per-line verdicts are settled.
- **Matching.** A returned path is named when some surviving line's `existing:` clause or some amend line's `excise <target>` contains that repo-relative path as text. A `tests/<file>::<test>` citation names `tests/<file>`, in an `existing:` clause and in an excise target alike: the test is in the file, so naming the test names the file. The `none, <the grep the main agent ran and its result>` form of `.claude/agents/gauntlet-arbiter.md:45` names a path only where its own grep output carries one, which is why the field becomes a list — a block whose closure is non-empty and whose every clause is a bare `none` has named nothing and takes the repair.
- **The verdict.** A returned path named by neither is `ANOTHER PASS`, one line per unnamed path, with the named repair: add the `existing:` clause or excise the test.
- **When it does not run.** Three readings, each printed rather than silent. An empty symbol set is `N/A`, no command; the block quotes no code in either field, so there is nothing to close over. No declared entry for the script in `.claude/hooks/blind-reads.json` is `N/A` too, which is what a repo that copied `.claude/` and never wired the script gets. The reviewer settles that by reading the file — the `allow` key above is what makes that read legal — and an unreadable or absent `blind-reads.json` is the same `N/A`, since a repo that declares nothing has wired nothing. A declared entry whose command fails or returns nothing readable is `ANOTHER PASS` naming the failure, never a silent pass — a closure that did not run is not a closure that came back empty. A closure that ran and returned nothing is a pass: `tests/support` holds no Python module in this repo, so the hop contributes nothing here, and an empty return is the ordinary reading for a block whose symbols no test references yet.

Block-level rather than per-line is a deliberate choice and the check says so. The symbol set is the block's, drawn from every line's fields together, so the closure's answer cannot be attributed to one line, and a `STRICKEN <letter>` would cut a line for a file some other line's symbol reached. `.claude/agents/gauntlet-arbiter.md:134` already carries a block-level clause of this shape inside check (m) — a brief sentence no surviving line pins is named in the `ANOTHER PASS` repair — so the verdict route exists and is not new.

**`docs/approved-specs.md:38`** and the prose beneath the fence at `docs/approved-specs.md:29-44`. The field line takes the list form, and a short paragraph after `docs/approved-specs.md:46` says what the field now answers: the outcome grep, backward, and the closure, forward, with the closure named as the arbiter's block-level check rather than restated as a rule of its own. Full English, soft-wrapped one logical line per paragraph, per this repo's markdown gates.

**`.claude/hooks/blind-bash.py:9` and `docs/agents.md:26`.** The sentence at `.claude/hooks/blind-bash.py:9` naming the `gauntlet-arbiter` among the agents with no `Bash` is corrected to name the `gauntlet-juror` alone, which `.claude/agents/gauntlet-juror.md` still makes true. `docs/agents.md:26` drops the `gauntlet-arbiter` from its `blind-bash.py` clause and says what actually bounds that agent's shell under the switch: `reviews-lane.py`, `no-impl-reads.py`, `specs-lane.py` and `bwrap-wrap.py`. Prose only in both; `blind-bash.py`'s `BLIND` tuple and its three argument grammars do not move, and its `--self-test` pins none of that prose.

**`CHANGELOG.md`, under the `### Added` at `CHANGELOG.md:7`.** Two bullets: `scripts/gates/symbol-closure.py` with the declared-run entry that lets the arbiter call it, and the arbiter's closure check with the `existing:` list form. They land because `.claude/agents/gauntlet-arbiter.md` and `.claude/hooks/blind-reads.json` change and both are kit, which is the bar `CLAUDE.md:7`. The first bullet says in as many words that the check needs `scripts/gates/symbol-closure.py` copied too, since a consumer taking `.claude/` and `docs/` alone does not get it. The script's `--self-test` and its gate entry are named inside that bullet rather than as an entry of their own, because `CLAUDE.md:9` is "**Tests and test policy never go in it.**"

## Caller-side delta

Three hooks stand between the arbiter and that command, and all three key on the same predicate, so the single JSON entry is the whole wiring.

`.claude/hooks/tests-lane.py:94` is `return _BASH if sh.lane_write_in(command, BASH_TESTS) else None`, so the lane denies a command only where a stage that names a `tests/` path also writes. `.claude/hooks/shell_shapes.py:486` returns `False` from `segment_writes` for any stage `is_runner` recognizes, and `.claude/hooks/shell_shapes.py:406` puts the declared table first inside `is_runner`. The `printf` stage in front is already read-only at `.claude/hooks/shell_shapes.py:60`. That is why `find tests -maxdepth 2` comes back denied in this session while the declared run will not: `find` is on neither list.

`.claude/hooks/no-impl-reads.py:360` is `if sh.is_runner(words) or (head in runners and not inline):`, so the same predicate carries the invocation past the read block. It has to: `.claude/hooks/no-impl-reads.py:88` allows `docs/`, `tests/`, `state/` and `gauntlet/specs/approved/`, and `scripts/` is on none of them — the arbiter runs the script and never reads it.

`.claude/hooks/reviews-lane.py:49` says a run counts as read-only in every form `shell_shapes.is_runner` recognizes, and `.claude/hooks/reviews-lane.py:51` names the invocations this repo declares in `blind-reads.json` as that widening. So the reviewer's own lane admits it without a carve-out.

`.claude/hooks/specs-lane.py` and `.claude/hooks/bwrap-wrap.py` are unaffected: no write is proposed outside the reviewer's two folders, and the command is an ordinary one the sandbox already wraps.

Nothing outside the arbiter calls the script. `scripts/pair.sh`, `scripts/excision-diff.py` and `scripts/cite.py` are untouched, and no agent but the `gauntlet-arbiter` is given the entry.

## What it costs

**A shipped agent definition names a `scripts/gates/` path for the first time, and that departs from `CLAUDE.md:46`.** That line is "Both gates are this repository's own, which is why they sit in `scripts/gates/` and are wired from `settings.local.json`. `.claude/hooks/` and `.claude/settings.json` are the kit a consumer copies, and nothing that only matters here goes in them." The two gates it speaks of are the markdown gates, and `scripts/gates/` has held only things that matter here. `symbol-closure.py` does not: `.claude/agents/gauntlet-arbiter.md` is kit, and the check written into it names `scripts/gates/symbol-closure.py` outright. No shipped agent names a `scripts/gates/` path today — `grep -rn "scripts/gates" .claude/agents/` returns nothing. Shipped agents do name `scripts/`: `grep -rn "scripts/" .claude/agents/*.md` returns thirteen hits, `.claude/agents/gauntlet-arbiter.md:56` among them, which names `scripts/pair.sh open`. It is the gates directory that has held only things that matter here, and it is that line the change crosses. The owner's brief fixes the location and the gate entry, so the plan takes both and states the departure rather than relocating the script into `.claude/hooks/`: `scripts/gates/` stops being purely this repo's own, and the kit a consumer copies becomes `.claude/`, `docs/` and that one script. The `N/A` reading above is what keeps a consumer who takes less than that from being stuck, and it is the reason that reading exists.

**The anchor argument is a wart, and it is the declared-run shape that forces it.** `.claude/hooks/shell_shapes.py:306` expands a prefix into a shape requiring at least one character after the separator, so `tests/` is not a legal argument and neither is anything that normalizes to bare `tests`. The scan root therefore arrives as a path inside the tree rather than as the tree. `tests/__init__.py` is tracked and is the tests package marker, so it is the stable choice, but a reader of the arbiter's command sees a file path where the script takes a directory. Widening the shape to admit the bare prefix is the alternative and is not taken here: it is a change to a hook the whole lane rests on, for a cosmetic gain.

**Symbols on stdin put a pipeline in a blind agent's shell.** The declared run carries exactly one free argument, so the symbol set cannot travel in argv. The `printf` stage is read-only and the lane admits it, but the arbiter's command is no longer a single stage, and a reviewer that appends a second command after the closure is outside what any hook here checks — `.claude/hooks/blind-bash.py` is the hook that matches whole command text end to end, and it does not answer for the arbiter.

**File granularity over-reports, deliberately, and the block pays for it.** A symbol name that collides with an unrelated local in an unrelated test file puts that file in the closure, and the block must then name it or excise it. The bound is stated rather than worked around: no arity analysis, no scope analysis, no resolution of what a name actually binds to. The cost lands as extra `existing:` clauses on blocks that touch common names, and the repair for a false hit is to name the file, which is cheap and honest.

**One transitive hop through `tests/support` is, today, untested by the live tree.** `tests/support` currently holds `fixtures/cite/` and no Python module at all, so the hop has nothing to walk in this repo and is exercised only by the `--self-test`'s own temporary fixtures. It is built now because the brief asks for it and because a support module added later would otherwise break the closure silently.

**The gate is the only bar on this script until a spec block pins it.** `tests/` is the `gauntlet-scrivener`'s lane and the main agent is denied every write there, so the pytest suite gains nothing from this change. `scripts/gates/check-gates.sh:36` runs `pytest` over `tests/`, and the new gate's `--self-test` runs beside it; between them the `--self-test` carries the whole bar. Pinning the closure in `tests/` needs an approved spec block and the blind writer, which is a separate round of the chain and is not planned here.

**Every round of review costs one more command, and it is a small one.** The arbiter runs the closure once per block, plus once more per re-review round that changes a line's `bite:` or `existing:` text. `git ls-files | grep -c '^tests/.*\.py$'` is 5, so the scan is five `ast.parse` calls on files of a few hundred lines — far below the disclosure floor of one core for ten seconds at `/home/atom/dev/CLAUDE.md:23`, and nothing is owed for it. What is new is a dependency of the review round on a script being present and executable: a checkout that declares the entry but lacks the file gives the reviewer a failed command, which the check routes to `ANOTHER PASS` naming the failure rather than to a silent pass.

**Deliberately left out.** No change to checks (a) through (t), no renumbering, no change to the `gauntlet-scrivener`, the `gauntlet-bailiff` or `docs/testing.md`. The closure is not wired as a `Stop` gate and not run by `scripts/pair.sh`: the brief puts it in the arbiter's hands and nowhere else.

## Open questions

None.

--- reviewer ---
READY

Round 3's (b6) is repaired and withdrawn: the cost element's heading now reads "**A shipped agent definition names a `scripts/gates/` path for the first time, and that departs from `CLAUDE.md:46`.**", which is the claim its own body supports. No finding stands.

a  PASS  carried: PASS  Redefinition of `existing:` is stated outright as the change proposed, not as a means: "`existing:` carries more than one clause from here on ... it becomes a list", and the owner's own brief sentence 3 authorizes it ("`existing:` now answers two questions rather than one"). The field's current grammar resolves at `.claude/agents/gauntlet-arbiter.md:45` and `docs/approved-specs.md:38`, both named as changing.

b  PASS  The heading sentence now says `scripts/gates/`, which is what `grep -rn "scripts/gates" .claude/agents/` (nothing) and `grep -rn "scripts/" .claude/agents/*.md` (thirteen lines, `gauntlet-arbiter.md:56` carrying `scripts/pair.sh open`) together support. The round's other new sentence resolves too: `.claude/hooks/no-impl-reads.py:156` builds the allow list from the config rather than from the caller, and `docs/agents.md:24` is "The `gauntlet-arbiter`, the `gauntlet-scrivener`, the `gauntlet-juror` and the `gauntlet-bailiff` are the four that never read the implementation." Every citation in the plan has been opened across four rounds and each says what its sentence says.

c  PASS  carried: PASS  Nothing is split, extracted or moved; the one grammar widening is `existing:` to a list, and I counted its readers: `gauntlet-scrivener.md:66` and `:102` already read the field as clauses plural, `docs/testing.md:79` only exempts strike lines, `grep -rn "existing:" scripts/` returns nothing, and the single `tests/` hit is fixture text. Matches the plan's count.

d  PASS  carried: PASS  Cause named upstream of appearance: no stage asks which tests the change reaches, check (b) at `gauntlet-arbiter.md:110` is backward-looking by construction, and the merge-time breakage follows from that.

e  PASS  carried: PASS  The rules touched are all named and the two departures stated rather than absorbed: `CLAUDE.md:46` quoted verbatim in a cost element that says what it overturns, `docs/agents.md:26` and `blind-bash.py:9` named as the stale prose that changes, and now the blind-read allowlist itself, widened by one file with the `gauntlet/` carve-out at `no-impl-reads.py:205-214` named as untouched. I checked the one rule the plan does not name and it holds: `_no_denied_nesting` at `no-impl-reads.py:438` computes over the real config's `allow` list, and an entry of `.claude/hooks/blind-reads.json` is neither an ancestor of `gauntlet` nor under it, so that gate stays green.

f  PASS  carried: PASS  All four boundaries are in the check text and all four are now readings the reviewer can take. The unwired case is settled by reading `.claude/hooks/blind-reads.json`, which the new `allow` key makes legal: `readable()` at `no-impl-reads.py:217-223` matches an entry with no trailing slash by exact relative path, so that one file and nothing else beside it opens. An unreadable or absent file takes the same `N/A`. The item's tally is right as written — the three readings under "When it does not run" are the empty symbol set, the unwired repo and the failed command; the fourth sentence is the case where it did run.

g  PASS  carried: PASS  Open questions is `None`, no padding, and I found no question a document already answers nor one undecidable without the owner. The declared-shape-widening alternative is decided in-plan with its reason.

h  PASS  The consumer claim has a mechanism that holds one hop, the gate-count claim names both sites and there is no third, and the one reach I traced wider than the plan stated is now stated in the plan: the `allow` entry opens that file to all four blind agents, it carries no implementation citation, and `docs/agents.md:24` holds for all four. Nothing I can reach from the changed symbols arrives at anything the plan calls untouched.

i  PASS  carried: PASS  Four brief sentences, four `delivered:` lines, nothing `dropped:`, Open questions `None`. The third line is now accurate: the `gauntlet-arbiter.md:45` element carries the list grammar, and the `gauntlet-arbiter.md:48` and `docs/approved-specs.md` elements state the two questions. Sentence 4 resolves to the artifact itself, which is the only thing it could name.

j  N/A   carried: N/A   This repo names no composition root in any architecture doc.

k  PASS  carried: PASS  The Matching item now states the rule and reads as brief sentence 2 reads. A returned path is named by textual containment in a surviving line's `existing:` clause or an amend line's `excise <target>`; a `tests/<file>::<test>` citation names `tests/<file>` in both, which is consistent with `.claude/agents/gauntlet-arbiter.md:96` and with the whole-file target `STRICKEN` at `:98`; the `none, <grep>` form names a path only where its own output carries one, and a block whose closure is non-empty and whose every clause is a bare `none` takes the repair. A `motion: strike` block, where `.claude/agents/gauntlet-arbiter.md:94` runs no per-line check but (m) and no `bite:` or `existing:` field exists, derives no symbols and lands on the empty-set reading, which is consistent.

Notes, for the owner rather than for the plan:

1. `git` is denied to me in every form but an object restore, by `hooks/reviews-lane.py`, so neither `git ls-files tests/__init__.py` nor the footprint count was re-run in any round. Both cross-check against `ls tests/`, which shows `__init__.py` and four `test_*.py` files and no `.py` under `tests/support`: five files, the number the plan carries.
2. `cite.py --check` was not reproduced here. It resolves citations; whether a cited line supports its sentence is what (b) turns on, and that was read by hand.
3. Two things this plan leaves for a later round, both stated in it rather than hidden: the closure is pinned by its own `--self-test` and by no test under `tests/` until a spec block goes through the chain, and the `tests/support` hop has nothing to walk in this repo today.
