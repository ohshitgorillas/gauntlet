slug: fail-open-bypasses
kind: new
brief:
> The three fail-open bypasses plus their two adjacent classification bugs. All in .claude/hooks/shell_shapes.py + .claude/hooks/no-impl-reads.py. No edits made.
>
> In scope
>
> 1. shell_shapes.py:64 REDIRECT — allow a file-descriptor prefix and &>. Closes cat impl.py 1> tests/t.py, 2> specs/approved/s.txt, 1> state/reviews/s.9.txt across all three lanes. Keep 2>&1 and >&2 non-writes.
> 2. shell_shapes.py:69,136 heredoc bodies — segments() drops the body before any check, so python3 - <<EOF names its target where nothing looks. Pair each stage with the body opened on its line; search the body only for stages that already classify as writes, so cat <<EOF prose stays prose.
> 3. no-impl-reads.py:232 git — git show <rev>, git log -p, git diff <rev>, git stash show -p print the implementation and carry no path-shaped word. Metadata subcommands pass; anything that prints content needs an allowlisted path; unknown subcommand treated as content. Must keep git show <rev>:specs/approved/<slug>.txt working — strip <rev>:.
> 4. shell_shapes.py:49 -p — INLINE_SCRIPT holds -p/--print for perl/node, so pytest -p no:cacheprovider tests/ is denied as a write. Read the flag against the head word. Same helper adds bare - as an inline script.
> 5. shell_shapes.py:155 pytest with arbitrary args — pytest --junitxml=tests/out.xml is a lane write classified as a run. Small flag-prefix list disqualifies the run.
>
> Owner ruling during planning: a redirection whose target is exactly /dev/null is not a write, tested per redirection.

Surface under test: the wire surface of the PreToolUse hooks in .claude/hooks/. One JSON object on stdin carrying `tool_name`, `tool_input` and `cwd`. The hook answers on stdout with either nothing at all, written SILENT below, which is the call being allowed, or a JSON object whose `hookSpecificOutput.permissionDecision` is `deny`, written DENY below. The hooks are invoked as `python3 .claude/hooks/<hook>.py` with the payload on stdin. Every `bite:` value below was measured that way by a gauntlet-accountant, by a script that ran each payload through a subprocess and read stdout; every invocation exited rc=0 with empty stderr. Every payload carries `tool_name` `Bash`, `tool_input` `{"command": <the command>}` and cwd /home/atom/dev/gauntlet.

1. tests-lane.py answers DENY for `cat impl.py 1> tests/t.py`, for `cat impl.py 2> tests/t.py`, for `cat impl.py &> tests/t.py`, for `cat impl.py > tests/t.py 2>/dev/null` and for `cat impl.py > tests/nullish`, and SILENT for `grep x tests/t.py 2>&1`, for `echo hi >&2` and for `cat tests/t.py 2>/dev/null`; specs-lane.py answers DENY for `cat impl.py 1> specs/approved/s.txt`; reviews-lane.py answers DENY for `cat impl.py 1> state/reviews/s.9.txt` on a payload carrying no `agent_type` key.
   kills: reads a redirection only when no file descriptor precedes it, so an agent that cannot write a test with the Edit tool writes one with `cat impl.py 1> tests/t.py` and the lane answers nothing, while the same agent's `2>&1` on a plain read is refused.
   bite: SILENT at `cat impl.py 1> tests/t.py`; SILENT at `cat impl.py 2> tests/t.py`; SILENT at `cat impl.py &> tests/t.py`; DENY at `cat impl.py > tests/t.py 2>/dev/null`; DENY at `cat impl.py > tests/nullish`; SILENT at `grep x tests/t.py 2>&1`; SILENT at `echo hi >&2`; SILENT at `cat tests/t.py 2>/dev/null`; SILENT at `cat impl.py 1> specs/approved/s.txt` on specs-lane.py; SILENT at `cat impl.py 1> state/reviews/s.9.txt` on reviews-lane.py.
   note: the reviews-lane payload carries no `agent_type` key on purpose. The same command with `"agent_type": "gauntlet-arbiter"` measures DENY at HEAD, because that hook refuses a reviewer's metered shell whatever the command classifies as, so it would pin nothing about the redirection.
   existing: none on these inputs. `tests/test_hook_wire.py:109-131` drives tests-lane.py through five writing shapes, none of them carrying a redirection of any kind; no test in the repository sends `1>`, `2>`, `&>`, `2>&1`, `>&2` or `/dev/null` to any hook, and no test drives reviews-lane.py with a Bash payload.

2. tests-lane.py answers DENY for the three-line command `python3 - <<EOF`, `open('tests/t.py','w')`, `EOF`, for the same command with the closing `EOF` absent, for the same command whose delimiter is `'EOF-1'` on both lines, and for the two-stage command `echo hi && python3 - <<EOF`, `open('tests/t.py','w')`, `EOF`, whose heredoc is opened on the second stage; and SILENT for the three-line command `cat <<EOF`, `prose about tests/t.py`, `EOF`.
   kills: drops every heredoc body before anything looks at it, so the file a heredoc names is named where no check can see it and `python3 - <<EOF` writing a test is allowed, while a heredoc whose delimiter merely carries a hyphen is refused for the same body.
   bite: SILENT at the `<<EOF` write; SILENT at the unterminated `<<EOF` write; DENY at the `<<'EOF-1'` write; SILENT at the two-stage `<<EOF` write; SILENT at the `cat <<EOF` prose.
   existing: none on these inputs. No test in the repository sends a heredoc to any hook; `tests/test_hook_wire.py:120,144` carry `node -e` payloads, which are the nearest shape and carry no heredoc.

3. no-impl-reads.py answers DENY for `git show HEAD`, for `git log -p`, for `git diff HEAD~1`, for `git stash show -p`, for `git frobnicate` and for `git cat-file -p HEAD`, and SILENT for `git status`, for `git log --oneline` and for `git show HEAD:specs/approved/pair-sh.txt`.
   kills: scores a git command only by the path-shaped words it carries, so `git show HEAD` prints the implementation to a blind agent because it names no path, while `git show HEAD:specs/approved/pair-sh.txt` is refused because the revision prefix makes the allowlisted spec look like a filename.
   bite: SILENT at `git show HEAD`; SILENT at `git log -p`; SILENT at `git diff HEAD~1`; SILENT at `git stash show -p`; SILENT at `git frobnicate`; SILENT at `git cat-file -p HEAD`; SILENT at `git status`; SILENT at `git log --oneline`; DENY at `git show HEAD:specs/approved/pair-sh.txt`.
   existing: none on these inputs. No test in the repository sends any git command to any hook.

4. tests-lane.py answers SILENT for `pytest -p no:cacheprovider tests/` and for `pytest -c pytest.ini tests/`, and no-impl-reads.py answers SILENT for `pytest -c pytest.ini tests/` and DENY for `python -c "print(open('src/core.py').read())"`, for `node -e "console.log(require('fs').readFileSync('src/core.py','utf8'))"` and for `python3 - < src/core.py`.
   kills: reads an inline-script flag without looking at the head word in front of it, so the two standard ways to run pytest — with a config file and without a cache directory — are refused as writes by the lane, while the flag keeps doing its real job on `python -c` and `node -e`.
   bite: DENY at `pytest -p no:cacheprovider tests/` on tests-lane.py; DENY at `pytest -c pytest.ini tests/` on tests-lane.py; DENY at `pytest -c pytest.ini tests/` on no-impl-reads.py; DENY at `python -c "print(open('src/core.py').read())"`; DENY at `node -e "console.log(require('fs').readFileSync('src/core.py','utf8'))"`; DENY at `python3 - < src/core.py`.
   existing: `tests/test_hook_wire.py:133-152` and `:159-176` pin that inline scripts are denied and runner invocations pass, on `node -e` and `node --test` payloads. Neither sends a `pytest` invocation carrying any flag, so neither fails when the flag is read without its head word.

5. tests-lane.py answers DENY for `pytest --junitxml=tests/out.xml`, for `pytest --cov-report=html:tests/cov tests/` and for `pytest --basetemp=tests/tmp tests/`, and SILENT for `pytest tests/ -q`.
   kills: accepts `pytest` with any arguments at all as a run of the suite, so an agent writes into the lane with the runner the lane is built to let through, by handing pytest an output path instead of a command of its own.
   bite: SILENT at `pytest --junitxml=tests/out.xml`; SILENT at `pytest --cov-report=html:tests/cov tests/`; SILENT at `pytest --basetemp=tests/tmp tests/`; SILENT at `pytest tests/ -q`.
   past the cap because: this line and line 4 are two different mechanisms on the same head word, and neither implementation passes the other's inputs. Reading the inline-script flag against the head word, which is all line 4 asks, leaves `pytest` accepted with arbitrary arguments, so `pytest --junitxml=tests/out.xml` is still a run and this line still fails. Disqualifying a run on an output-writing flag prefix, which is all this line asks, says nothing about whether `-c` and `-p` are inline scripts, so `pytest -c pytest.ini tests/` is still denied and line 4 still fails.
   existing: none on these inputs. `tests/test_hook_wire.py:133-152` pins that runner invocations pass the lane, and sends no pytest invocation carrying an output-path flag, so it passes at HEAD and would pass against an implementation that never disqualifies a run.

--- reviewer ---
READY
discriminates: sweep on the PreToolUse hook decision wire (DENY vs SILENT over contrasting commands on one hook)

1  KEEP  the eight tests-lane redirection commands plus `cat impl.py 1> specs/approved/s.txt` on specs-lane.py and `cat impl.py 1> state/reviews/s.9.txt` on reviews-lane.py (no `agent_type` key) -> DENY, against `grep x tests/t.py 2>&1`, `echo hi >&2`, `cat tests/t.py 2>/dev/null` -> SILENT; reading a redirection only when no file descriptor precedes it fails it at `cat impl.py 1> tests/t.py`; bite: SILENT at `cat impl.py 1> tests/t.py`, SILENT at both new cross-lane inputs. Verdict moves off DELTA because the two new inputs drive specs-lane.py and reviews-lane.py, which the dict at tests/test_hook_wire.py:109 cannot hold and no existing test covers; the eight tests-lane inputs still fold into :109, so the new test is the cross-lane one. The `agent_type` exclusion is correct and needs no change: the payload carrying `"agent_type": "gauntlet-arbiter"` measures DENY at HEAD, so it has no bite and would pass an implementation that never changes.
2  DELTA tests/test_hook_wire.py:109: the four heredoc commands plus the two-stage `echo hi && python3 - <<EOF` are five more entries in the existing `expected` dict of test_writes_under_tests_are_denied_in_every_shell_shape -- same hook, same bash_payload/REPO_CWD, same whole-dict assertion; the `cat <<EOF` prose case is its SILENT half.
3  KEEP  `git show HEAD`, `git log -p`, `git diff HEAD~1`, `git stash show -p`, `git frobnicate`, `git cat-file -p HEAD` -> DENY and `git status`, `git log --oneline`, `git show HEAD:specs/approved/pair-sh.txt` -> SILENT on no-impl-reads.py; scoring a git command only by its path-shaped words fails it at `git show HEAD` (SILENT where DENY is required) and at the `HEAD:specs/...` input (DENY where SILENT is required); bite: SILENT at `git show HEAD`, SILENT at `git frobnicate`, DENY at `git show HEAD:specs/approved/pair-sh.txt`. existing: none confirmed -- tests/test_hook_wire.py is the only file that drives a hook and it sends no git payload.
4  DELTA tests/test_hook_wire.py:133 (carried, line unchanged): `pytest -p no:cacheprovider tests/` and `pytest -c pytest.ini tests/` -> SILENT are two more entries in test_runner_invocations_pass_the_lane_but_inline_scripts_do_not; the no-impl-reads half folds the same way into :159, where the `python -c` and `node -e` DENY entries already sit verbatim.
5  DELTA tests/test_hook_wire.py:109: `pytest --junitxml=tests/out.xml`, `pytest --cov-report=html:tests/cov tests/` and `pytest --basetemp=tests/tmp tests/` -> DENY are three more entries in the every-shell-shape dict; `pytest tests/ -q` -> SILENT is already pinned there by the runner entries at :139-141. Round 1 CUT withdrawn: the DENY side is now three distinct output-writing flags, so a stub keyed to one literal fails the line, and a stub carrying all three is the flag-prefix list the brief asks for rather than a shortcut around it.

null stub: every hook writes nothing to stdout for every payload -- no classification, no decision, SILENT always.
  satisfies: none   fails: 1, 2, 3, 4, 5
hard-coded stub: a token table over the command string -- deny on `1>`/`2>`/`&>`, on `<<`, on `git show`/`git log -p`, on `-c`/`-p`, on `--junitxml`; no head-word, no delimiter, no target, no subcommand and no revision parsing.
  satisfies: none   fails: 1 (denies `grep x tests/t.py 2>&1` and `cat tests/t.py 2>/dev/null`, which must be SILENT), 2 (denies `cat <<EOF` prose, which must be SILENT), 3 (denies `git show HEAD:specs/approved/pair-sh.txt` and allows `git frobnicate`), 4 (denies `pytest -c pytest.ini tests/`, which must be SILENT), 5 (allows `pytest --cov-report=html:tests/cov tests/` and `pytest --basetemp=tests/tmp tests/`)

survives: 2

note: the approved block below was composed from the round-1 block text plus the repairs as the return stated them; the main agent's copy must match it line for line before the red run.
note: `.claude/hooks/reviews-lane.py` carries an uncommitted edit (6 insertions, 4 deletions against HEAD) at the time of measurement; the accountant reports the reviews-lane bite is the same against HEAD and the on-disk file. I cannot read either, so this stands as the owner's to confirm.
